const DEFAULT_CONFIG = {
    stopConfirmationTime: 3 * 60 * 1000,
    candidateRadius: 35,
    stopExitRadius: 70,
    maxAccuracy: 100,
    minPositionInterval: 5000,
    storageKey: "tracker_night_state",
    autoSave: true
};
